#pragma once

// Brings in INDEX_PACKAGE_API + the standard package entry-point declarations.
// Kept as a thin re-export header so existing #includes elsewhere in the
// package still work.
#include "Packages/PackageApi.hpp"
